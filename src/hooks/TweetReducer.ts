import { ITweet } from "@/types/Tweet";

export  const TweetReducer = ({state,action}:{state:ITweet,action:})=>{
    switch (action.type) {
        case "SET_IMAGE_URL":
            
            break;
    
        default:
            break;
    }
}